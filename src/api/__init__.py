"""
Fast API appplication instance for the orchestrator service.
This module initializes and configures the FastAPI application,
including middleware, routes, and other settings.

It serves as the entry point for the orchestrator service,
handling incoming HTTP requests and routing them to the appropriate handlers.
"""